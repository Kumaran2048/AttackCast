# Model Card

The world model is a PyTorch GRU with an MLP feature encoder, attention pooling and an 8-state prediction head.

Intended use: defensive research and human-in-the-loop advisory forecasting.

Out of scope: automatic blocking, autonomous response, or claims of causal intervention.

Synthetic development results must never be represented as real-world evaluation.
